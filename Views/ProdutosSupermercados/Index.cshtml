@model IEnumerable<ZeroWaste.Models.ProdutosSupermercado>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supermercado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supermercado.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDProdutosSupermercado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDProdutosSupermercado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDProdutosSupermercado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
